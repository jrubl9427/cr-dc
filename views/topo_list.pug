extends layout

block content
    h1= title
    
    ul
        each topo in topo_list
            li
                a(href=topo.url) name: #{topo.name}
                | (Rating: #{topo.rating})
                
        else
            li There are no topos.