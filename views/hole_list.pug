extends layout

block content
    h1= title
    
    ul
        each hole in hole_list
            li
                a(href=hole.url) Hole Number: #{hole.name}
                //- | (#{hole.course.name})
                
        else
            li There are no holes.