extends layout

block content
    h1 Dogleg: #{dogleg.name} Delete
    
    p Do you really want to delete this dogleg?
        
        form(method='POST' action='')
            div.form-group
                input#id.form-control(type='hidden' name='doglegid', required='true' value=dogleg._id)

            button.btn.btn-primary(type='submit') Delete