extends layout

block content
    h1= title
    
    ul
        each obstacle in obstacle_list
            li
                a(href=obstacle.url) obstacle: #{obstacle.name}
                
        else
            li There are no obstacles.