extends layout

block content
    h1 layup: #{layup.name} Delete
    
    p Do you really want to delete this layup?
        
        form(method='POST' action='')
            div.form-group
                input#id.form-control(type='hidden' name='layupid', required='true' value=layup._id)

            button.btn.btn-primary(type='submit') Delete