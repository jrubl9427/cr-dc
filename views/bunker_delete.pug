extends layout

block content
    h1 Bunker: #{bunker.name} Delete
    
    p Do you really want to delete this bunker?
        
        form(method='POST' action='')
            div.form-group
                input#id.form-control(type='hidden' name='bunkerid', required='true' value=bunker._id)

            button.btn.btn-primary(type='submit') Delete