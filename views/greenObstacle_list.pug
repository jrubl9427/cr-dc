extends layout

block content
    h1= title
    
    ul
        each greenObstacle in greenObstacle_list
            li
                a(href=greenObstacle.url) greenObstacle: #{greenObstacle.name}
                
        else
            li There are no greenObstacles.